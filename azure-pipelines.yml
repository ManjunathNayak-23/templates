# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest


resources:
  repositories:
    - repository: helpers
      type: git
      name: templates
      endpoint: github.com_ManjunathNayak-23
      
steps:
  - template: username.yml@helpers
    parameters:
      username: "chandu"